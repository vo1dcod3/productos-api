# Configuración exclusiva de pruebas: base H2 en memoria.
# Hace que los tests (incluido @SpringBootTest) sean autocontenidos, sin depender
# de un PostgreSQL real ni de Docker — funcionan igual en local y en CI.

spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# JWT — valores de prueba (no son secretos reales de producción)
jwt.secret=uUmmOg0dohj/JCi7V0WY5EEECAg3HoAZAdaj2FPLRftM/yGGZXKHSKTRNNywXbeark3qgcJ15Mcx74vUdNHygA==
jwt.expiration=86400000
